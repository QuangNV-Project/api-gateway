spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
    name: api-gateway
  cloud:
    config:
      uri: ${CONFIG_SERVICE_URL}
      username: ${CONFIG_NAME}
      password: ${CONFIG_PASSWORD}
      fail-fast: true